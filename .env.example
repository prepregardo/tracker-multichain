# Database (Neon PostgreSQL — provisioned via Vercel Storage)
DATABASE_URL=
DIRECT_URL=

# NextAuth
NEXTAUTH_SECRET=
NEXTAUTH_URL=https://your-app.vercel.app

# Admin email (gets ADMIN role on first login)
ADMIN_EMAIL=admin@tracker.local

# Blockchain Providers (optional — add later)
ETHERSCAN_API_KEY=
TRONGRID_API_KEY=
